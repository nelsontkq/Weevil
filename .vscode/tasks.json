{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --preset default && cmake --build --preset default",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}